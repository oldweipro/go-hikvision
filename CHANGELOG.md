## 2026-09-03

新增 `-i` 命令行参数，支持指定搜索使用的网卡（多网卡环境下强制组播探测从该网卡发出，并只监听该网卡的回复）。

```bash
go run main.go -i eth3
```

不指定参数时保持默认行为，监听所有网卡。新增依赖 `golang.org/x/net`（用于设置组播出口网卡）。

## 2023-06-06

设置读取超时时间,否则会持续阻塞，一般情况下2-3秒就接收完了，如果时间太短，可按需分配

```go
err := conn.SetReadDeadline(time.Now().Add(time.Second * 2))
```